from solver.solver import Solver


class MidacoSolver(Solver):

    def __init__(self, fstop=0.0, maxeval=50000, algostop=2, cores=1, focus=10, ants=0.0, kernel=0.0):
        super().__init__()
        raise NotImplementedError("Midaco solver not supported in open access version (Needs subscription key).")
